specVersion: "v1beta"
serviceId: "finance_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "finance_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
